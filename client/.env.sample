# Loaded automatically by `vite dev` / `vite build --mode development`.
# Left unset on purpose: api.js falls back to the relative '/api' path,
# which vite.config.js proxies to http://localhost:5000 in dev.
# VITE_API_URL=
